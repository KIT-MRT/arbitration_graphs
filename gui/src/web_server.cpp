/**
 * @brief Make sure to compile an object file.
 *
 * This is important for a clean install:
 * All Web App resources are installed into e.g. /usr/local/share/arbitration_graphs
 * This web root path is defined at compile time and compiled into the object file.
 */

#include "gui/web_server.hpp"